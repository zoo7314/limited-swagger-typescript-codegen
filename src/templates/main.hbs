/* eslint-disable */
{{#if imports.length}}
{{#each imports}}
{{> importDecl this}}
{{/each}}

{{/if}}
{{#each services}}
/** {{this.tag}} */
export const {{this.finalExportName}} = {{this.name}}
{{/each}}

