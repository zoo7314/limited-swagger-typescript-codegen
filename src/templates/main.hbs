/* eslint-disable */
{{#if imports.length}}
{{#each imports}}
{{> importDecl this}}
{{/each}}

{{/if}}
{{#each services}}
/** {{this.tag}} */
export const {{this.name}}Service = {{this.name}}
{{/each}}

