export type {{identifier}} = {
  {{#each properties}}
  {{#if this.description}}
  /** {{this.description}} */
  {{/if}}
  {{this.identifier}}{{#unless this.required}}?{{/unless}}: {{this.type.value}};
  {{/each}}
}