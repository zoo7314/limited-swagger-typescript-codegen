{{#if namespace}}
import * as {{namespace}} from '{{this.module}}'
{{else}}
import {{#if defaultExport}}{{defaultExport}}{{#if namedExports.length}}, {{/if}}{{/if}}{{#if namedExports.length}}\{ {{namedExportsJoined}} \}{{/if}} from '{{this.module}}'
{{/if}}