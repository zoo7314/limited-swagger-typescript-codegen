{{#each extracts}}
{{> objectSchema this }}
{{/each}}
/**
 {{#if summary}}
 * {{summary}}
 {{/if}}
 {{#if description}}
 * {{description}}
 {{/if}}
 */
export function {{identifier}}(
  {{#if parameters.length}}
  params: {{paramsType.value}},
  {{/if}}
  config?: ClientRequestConfig,
) {
  return client.request<ClientApiResponse<{{resType.value}}>>({
    {{#each this.requestConfigEntries}}
    {{> requestConfigEntry this}}
    {{/each}}
  })
}